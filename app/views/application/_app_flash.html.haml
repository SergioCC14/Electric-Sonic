- if !flash.blank?
  .flash
    - if flash[:error]
      .error
        =flash[:error]

    - elsif flash[:notice]
      .notice
        =flash[:notice]

  :javascript
    setInterval(function(){$('.flash').slideUp();},5000);
